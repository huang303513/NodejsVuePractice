var firstName = 'Michael';
var lastName = 'Jackson';
var year = 1958;

function multiply(x, y) {
    return x * y;
}

function addS(x, y) {
    return x + y;
}

export { firstName, lastName, year, multiply, adds as add };